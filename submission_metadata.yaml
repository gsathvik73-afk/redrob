# Redrob Hackathon — Submission Metadata

# ============================================================================
# Team identity
# ============================================================================
team_name: "Group"

primary_contact:
  name: "Sathvik"
  email: "saisathvik127@gmail.com"
  phone: "+91 7406885858"

team_members:
  - name: "Sai Sathvik Gullipalli"
    email: "saisathvik127@gmail.com"
    role: "Backend / design"
  - name: "Pratham Mudakavi"
    email: "pratkavi2006@gmail.com"
    role: "Backend"

# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/gsathvik73-afk/redrob"

sandbox_link: "https://huggingface.co/spaces/sath-speare/redrob-ranker"

reproduce_command: "python rank.py --candidates ./candidates.jsonl --out ./submission.csv --validate-redrob"

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "Intel MacBook Pro 2019"
  cpu_cores: 8
  ram_gb: 32
  python_version: "3.12.13"
  os: "macOS 26.5.1"
  uses_gpu_for_inference: false
  has_network_during_ranking: false
  pre_computation_required: false
  pre_computation_time_minutes: 0
  measured_full_run_wall_clock: "2:57.82 on the released 100,000-candidate JSONL"

# ============================================================================
# AI tools declaration
# ============================================================================
ai_tools_used:
  - "ChatGPT"
  - "Codex"

ai_usage_summary: |
  Used ChatGPT/Codex for implementation assistance, code review, documentation,
  local benchmarking, and sandbox setup. The submitted ranking step is
  deterministic and offline: it does not call hosted LLM APIs, does not use a
  GPU, and does not require network access.
# ============================================================================
# Approach summary
# ============================================================================
methodology_summary: |
  Deterministic offline ranker for the released Senior AI Engineer JD. The
  system scans candidates once, extracts evidence for production search,
  retrieval, ranking, recommendation, vector infrastructure, evaluation
  literacy, hands-on recency, product-company background, location fit,
  availability, and trust/corroboration signals. It down-weights services-only
  histories, nontechnical AI keyword stuffing, CV/speech-heavy profiles without
  NLP/IR evidence, long notice periods, stale engagement, inflated skill claims,
  and honeypot-like consistency failures. The same feature values generate
  grounded reasoning strings. The full 100K ranking runs under 5 minutes on CPU.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true
  reproduction_tested: true
